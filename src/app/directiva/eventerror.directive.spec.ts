import { EventerrorDirective } from './eventerror.directive';

describe('EventerrorDirective', () => {
  it('should create an instance', () => {
    const directive = new EventerrorDirective();
    expect(directive).toBeTruthy(); //ARREGLAR ESTA VAINA 
  });
});
